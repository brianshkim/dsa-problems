var findContentChildren = function(g, s) {
    g.sort((a,b) => a-b);
    s.sort((a,b) => a-b);
    let count = 0
    let j = 0
    for(let i = 0;i<s.length;i++){
      if(s[i]>=g[j]) {
        count++
        j++
      }
    }
    return count
};
