function diagonalDifference(arr) {
    // Write your code here
    let sum = 0;
    for (let i = 0;i<arr.length;i++){
        console.log(arr[i][i],arr[arr.length-1 - i][i] )
        sum+=arr[i][i]
        sum-=arr[arr.length-1 - i][i]
    }
    return Math.abs(sum)

}
